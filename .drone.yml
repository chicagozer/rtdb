kind: pipeline
type: docker
name: default

steps:
#- name: test    
#  image: node
#  commands:
#    - npm install 
- name: docker  
  environment:
    container: kube
  image: banzaicloud/drone-kaniko
  settings:
    username: chicagozer
    password:
      from_secret: docker_password
    repo: chicagozer/rtdb
    tags:
      - harness
